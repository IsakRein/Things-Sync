[project]
name = "things-sync"
version = "0.2.0"
description = "AppleScript-backed wrapper for Things 3: full CRUD over to-dos, projects, areas, tags, contacts."
readme = "README.md"
requires-python = ">=3.13"
authors = [{ name = "Isak Reinholdsson", email = "isak@isak.io" }]
dependencies = [
    "rich>=15.0.0",
    "typer>=0.24.1",
]

[project.optional-dependencies]
dev = ["pytest>=8.0.0"]

[project.scripts]
things = "things_sync.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/things_sync"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v"
